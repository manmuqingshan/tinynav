name: Build Docker Image (multi)

on:
  push:
    branches: [docker]
  workflow_dispatch:

jobs:
  build-arm64:
    runs-on: ubuntu-22.04-arm
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Perform Git LFS Checkout
        run: git lfs checkout

      - name: Extract short commit SHA
        run: echo "SHORT_SHA=${GITHUB_SHA::8}" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker Image (aarch64)
        run: |
          docker buildx build \
            --platform linux/arm64 \
            --tag uniflexai/tinynav:${SHORT_SHA}-arm64 \
            --cache-from type=gha,scope=tinynav-arm64 \
            --cache-to type=gha,mode=max,scope=tinynav-arm64 \
            --push \
            .

  build-amd64:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Perform Git LFS Checkout
        run: git lfs checkout

      - name: Extract short commit SHA
        run: echo "SHORT_SHA=${GITHUB_SHA::8}" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker Image (amd64)
        run: |
          docker buildx build \
            --platform linux/amd64 \
            --tag uniflexai/tinynav:${SHORT_SHA}-amd64 \
            --cache-from type=gha,scope=tinynav-amd64 \
            --cache-to type=gha,mode=max,scope=tinynav-amd64 \
            --push \
            .

  build-multiarch:
    runs-on: ubuntu-22.04
    needs: [build-amd64, build-arm64]
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract short commit SHA
        run: echo "SHORT_SHA=${GITHUB_SHA::8}" >> $GITHUB_ENV

      - name: Compose multi-arch manifest
        run: |
          docker buildx imagetools create \
            -t uniflexai/tinynav:${SHORT_SHA} \
            -t uniflexai/tinynav:latest \
            uniflexai/tinynav:${SHORT_SHA}-amd64 \
            uniflexai/tinynav:${SHORT_SHA}-arm64
